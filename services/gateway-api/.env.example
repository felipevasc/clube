# Gateway/BFF configuration
USERS_URL="http://localhost:3001"
BOOKS_URL="http://localhost:3002"
GROUPS_URL="http://localhost:3003"
FEED_URL="http://localhost:3004"
EVENT_TARGETS="http://localhost:3004/events"

# Sessions (cookie HttpOnly)
# Use a long random value in production (ex: 32+ bytes).
SESSION_SECRET=""
# Optional (default: 30 days)
SESSION_MAX_AGE_SECONDS="2592000"

# Dev-only username login
# Enabled by default in non-production; set to 0 to disable.
# Never enabled in production.
ALLOW_DEV_USERNAME_LOGIN="1"

# Google OAuth (optional)
# Configure these to enable "Continuar com Google" on /login.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# Redirect URI must match exactly what's configured in Google Cloud.
# Dev (Vite proxy): http://localhost:5173/api/auth/google/callback
# Prod (same origin): https://SEU_DOMINIO/api/auth/google/callback
GOOGLE_REDIRECT_URI="http://localhost:5173/api/auth/google/callback"
