{
  "name": "@clube/gateway-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --import tsx src/index.ts",
    "test": "vitest run",
    "db:setup": "node -e \"process.exit(0)\"",
    "db:generate": "node -e \"process.exit(0)\"",
    "db:migrate": "node -e \"process.exit(0)\"",
    "db:seed": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@clube/shared": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.7",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
